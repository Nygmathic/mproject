name: Auto News Poster

on:
  schedule:
    - cron: "0 8,20 * * *"  # 8am and 8pm UTC — 20 articles/day
  workflow_dispatch:

jobs:
  post-news:
    runs-on: ubuntu-latest

    permissions:
      contents: write
      actions: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: pip install feedparser requests

      - name: Check API keys
        env:
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
          GROQ_API_KEY:   ${{ secrets.GROQ_API_KEY }}
        run: |
          [ -n "$GEMINI_API_KEY" ] && echo "✅ Gemini key set (length: ${#GEMINI_API_KEY})" || echo "⚠️  Gemini key NOT set"
          [ -n "$GROQ_API_KEY" ]   && echo "✅ Groq key set (length: ${#GROQ_API_KEY})"   || echo "⚠️  Groq key NOT set"
          [ -z "$GEMINI_API_KEY" ] && [ -z "$GROQ_API_KEY" ] && echo "❌ No AI keys — aborting" && exit 1 || true

      - name: Fetch and post news
        env:
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
          GROQ_API_KEY:   ${{ secrets.GROQ_API_KEY }}
        run: python auto_news_poster.py

      - name: Commit and push new articles
        run: |
          git config --global user.name "Veridus Auto Poster"
          git config --global user.email "autopost@veridus.space"
          git add content/news/ .posted_articles.json
          git diff --cached --quiet || git commit -m "Auto: Add news articles $(date +'%Y-%m-%d %H:%M UTC')"
          git push

      - name: Trigger Hugo deployment
        run: |
          curl -X POST \
            -H "Authorization: token ${{ secrets.GITHUB_TOKEN }}" \
            -H "Accept: application/vnd.github.v3+json" \
            https://api.github.com/repos/${{ github.repository }}/actions/workflows/hugo.yml/dispatches \
            -d '{"ref":"main"}'